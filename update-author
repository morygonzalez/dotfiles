git filter-branch -f --commit-filter '
if [ "$GIT_AUTHOR_NAME" = "" ]; then
  GIT_AUTHOR_NAME=""
  GIT_AUTHOR_EMAIL=""
fi
if [ "$GIT_COMMITER_NAME" = "" ]; then
  GIT_COMMITTER_NAME=""
  GIT_COMMITTER_EMAIL=""
fi
git commit-tree "$@"
' HEAD
